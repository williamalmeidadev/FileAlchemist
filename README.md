# FileAlchemist
Aplicação web que converte imagens (PNG, JPG/JPEG, WebP) diretamente no navegador do usuário, sem upload, com suporte a conversão em lote e download (individual e ZIP). Privacidade total: os arquivos não saem do dispositivo.
